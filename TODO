// TODO-1: options -- Done
// TODO-2: setElementOptions()
// TODO-3: on(), onElement()
// TODO-4: ODLayer 3 events

// map(_data, function());


    generate() {
        return this._data.map(
            (data)=>{
                var ret = L.marker(
                    data.coordination, data.options
                )
                if d contains 'popup':
                    ret.bindPopup(xxxxx)
                return ret;
            }
        );
    }