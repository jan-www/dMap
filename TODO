// map(_data, function());


// 方格图


function foo(d, i) {

}


xxl.on('click', foo);

    generate() {
        return this._data.map(
            (data)=>{
                var ret = L.marker(
                    data.coordination, data.options
                )
                if d contains 'popup':
                    ret.bindPopup(xxxxx)
                return ret;
            }
        );
    }